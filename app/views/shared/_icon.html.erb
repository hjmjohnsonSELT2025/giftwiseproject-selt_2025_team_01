<%# Usage: render 'shared/icon', name: 'gift', size: 32, color: 'text-white', bg_color: 'bg-gradient-to-br from-pink-400 to-pink-500' %>
<div class="<%= bg_color %> p-<%= size / 8 %> rounded-xl shadow-md flex items-center justify-center">
  <svg class="w-<%= size / 2 %> h-<%= size / 2 %> <%= color %>" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
    <%# inline SVG paths depending on name %>
    <% if name == 'gift' %>
      <rect x="3" y="8" width="18" height="4" rx="1"></rect>
      <path d="M12 8v13"></path>
      <path d="M19 12v7a2 2 0 01-2 2H7a2 2 0 01-2-2v-7"></path>
    <% end %>
  </svg>
</div>
