<h1>Edit Gift Idea for <%= @recipient.name %></h1>

<%= render "form", gift_idea: @gift_idea, url: recipient_gift_ideas_path(@recipient), submit_label: "Update Gift Idea" %>

<%= link_to "Back to Recipient", recipient_path(@recipient) %>