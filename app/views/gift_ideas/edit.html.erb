<h1 class="text-center text-2xl font-bold text-gray-700 mb-6">Edit Gift Idea for <%= @recipient.name %></h1>

<div class="max-w-2xl mx-auto">
  <%= render "form", gift_idea: @gift_idea, submit_label: "Update Gift Idea" %>

  <div class="mt-4 text-center">
    <%= link_to "Back to Recipient", recipient_path(@recipient), class: "text-gray-500 hover:text-gray-700" %>
  </div>
</div>
