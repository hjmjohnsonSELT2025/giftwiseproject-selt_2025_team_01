<h1 class="text-3xl font-bold mb-6 text-center">
  Add Gift for <%= @event_recipient.recipient.name %> in <%= @event_recipient.event.name %>
</h1>

<div class="max-w-xl mx-auto bg-white p-6 rounded-2xl shadow-md">
  <%= render "event_recipient_gift_form",
             gift_idea: @gift_idea,
             url: event_recipient_gift_ideas_path(
               @event_recipient.event,
               @event_recipient.recipient
             ),
             submit_label: "Save Gift" %>
</div>

<div class="mt-6 flex justify-center">
  <%= link_to "Back to Event",
              event_path(@event_recipient.event),
              class: "px-4 py-2 text-indigo-700 border border-indigo-300 rounded-xl hover:bg-indigo-50 transition-all duration-200 font-medium" %>
</div>
